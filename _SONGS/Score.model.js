export class Score {
  constructor(data) {
    
    this.data = data;
    this.id = data.id;
    this.title = data.title;
    this.metadata = data.metadata;
  };
  get prop() { return this._prop };
  set prop(newValue) { this._prop = newValue };
}

const Score = {
  id: uuid,
  title: work => work - title,
  metadata: {
    ...identification,
    encoding => software,
    creator: { type, value }
  },
  parts: {
    P1: {
      name: partName,
      attributes: { id },
      scoreInstrument: { name, id },
      midiInstrument: { name, id, channel, program },
      scorePart: {
        id,
        measures
      }
    }
  }
  partList:

}